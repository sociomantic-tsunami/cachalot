### Upgrade of default dmd-transitional version

Starting with v7, default dmd-transitional provided by `develdlang` image is of
version `2.078.*`. If projects doesn't yet support it, install older version
explicitly.
